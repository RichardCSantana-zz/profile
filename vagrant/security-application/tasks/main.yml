---
# tasks file for ui-application
- name: Start a container
  docker_container:
    name: profile-application
    image: richardcsantana/profile-application
    state: started
    ports:
     - "8080:8080"
